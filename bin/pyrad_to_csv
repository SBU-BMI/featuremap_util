#!/usr/bin/env bash

PROGNAME=$(basename "$0")

if [ "$#" -ne 1 ]; then
  echo ""
  echo "usage: "
  echo "    ./$PROGNAME exec_id"
  exit 1
fi

exec_id="$1"
python /app/src/pyrad_to_map.py "/data/input" "/data/output" "$exec_id"
echo "$PROGNAME done."
